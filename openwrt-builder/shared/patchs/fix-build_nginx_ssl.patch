Index: immortalwrt/feeds/luci/collections/luci-nginx/Makefile
===================================================================
--- immortalwrt.orig/feeds/luci/collections/luci-nginx/Makefile
+++ immortalwrt/feeds/luci/collections/luci-nginx/Makefile
@@ -18,7 +18,7 @@ LUCI_DEPENDS:= \
 	+luci-mod-admin-full \
 	+luci-proto-ppp \
 	+luci-theme-bootstrap \
-	+nginx \
+	+nginx-ssl \
 	+nginx-mod-luci \
 	+rpcd-mod-rrdns
 
Index: immortalwrt/feeds/packages/net/nginx/Makefile
===================================================================
--- immortalwrt.orig/feeds/packages/net/nginx/Makefile
+++ immortalwrt/feeds/packages/net/nginx/Makefile
@@ -200,7 +200,7 @@ define Package/nginx-mod-luci
   SUBMENU:=Web Servers/Proxies
   TITLE:=Support file for Nginx
   URL:=http://nginx.org/
-  DEPENDS:=+uwsgi +uwsgi-luci-support +nginx +nginx-mod-ubus
+  DEPENDS:=+uwsgi +uwsgi-luci-support +nginx-ssl +nginx-mod-ubus
 endef
 
 define Package/nginx-mod-luci/description
@@ -403,7 +403,7 @@ endef
 define BuildModule
   define Package/nginx-mod-$(1)
     $(call Package/nginx/default)
-    DEPENDS:=+nginx $(2)
+    DEPENDS:=+nginx-ssl $(2)
     TITLE:=Nginx $(1) module
   endef
 
