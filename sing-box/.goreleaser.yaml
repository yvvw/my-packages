version: 2

project_name: sing-box

builds:
  - main: ./cmd/sing-box
    flags:
      - -trimpath
    ldflags:
      - -X github.com/sagernet/sing-box/constant.Version={{ .Version }}
      - -s
      - -w
      - -buildid=
      - -X internal/godebug.defaultGODEBUG=multipathtcp=0
      - -checklinkname=0
    tags:
      - with_utls # reality
      - with_tailscale # tailscale
      - with_gvisor # tailscale
      - with_musl
      - with_clash_api
      - with_tools_generate
    env:
      - CGO_ENABLED=0
    targets:
      - linux_arm64
      - linux_amd64
      - windows_amd64
    mod_timestamp: "{{ .CommitTimestamp }}"

archives:
  - formats:
      - tar.gz
    format_overrides:
      - goos: windows
        formats: zip
    name_template: >-
      {{ .ProjectName }}-{{ .Version }}-{{ .Os }}-{{ .Arch }}
      {{- if .Arm }}v{{ .Arm }}{{ end -}}
      {{- if and .Amd64 (ne .Amd64 "v1") }}{{ .Amd64 }}{{ end -}}
    wrap_in_directory: true
